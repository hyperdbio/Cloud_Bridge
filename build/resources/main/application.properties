# =====================================
# 🧭 애플리케이션 이름 (프로젝트 식별용)
# =====================================
# - 스프링부트가 내부적으로 서비스 이름을 구분할 때 사용.
# - 서버 로그나 클라우드 환경에서 "이 프로젝트가 어떤 앱인지" 구별할 때 쓰임.
spring.application.name=cloud_bridge


# =====================================
# ✅ MySQL 데이터베이스 연결 설정
# =====================================

# DB 드라이버(운전기사 역할)
# - 자바가 MySQL과 통신할 수 있도록 도와주는 연결 프로그램
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# DB 주소(URL)
# - jdbc:mysql://[서버주소]:[포트]/[DB명]
# - DB가 원격 서버(project-db-campus.smhrd.com)의 3307 포트에서 동작 중임
spring.datasource.url=jdbc:mysql://project-db-campus.smhrd.com:3307/campus_24IS_CLOUD3_p3_3

# DB 사용자 계정 (아이디)
spring.datasource.username=campus_24IS_CLOUD3_p3_3

# DB 비밀번호
spring.datasource.password=smhrd3


# =====================================
# ✅ JPA (Hibernate) 설정
# =====================================

# 테이블 자동 생성 / 업데이트 옵션
# - none : 아무 것도 하지 않음
# - update : Entity 변경 시, DB 테이블 구조 자동 수정 (개발 단계에서 주로 사용)
# - create : 기존 테이블 삭제 후 새로 생성 (데이터 다 사라짐 주의!)
# - create-drop : 프로그램 종료 시 테이블 삭제
spring.jpa.hibernate.ddl-auto=update
# spring.jpa.hibernate.ddl-auto=create

# 실행되는 SQL문 콘솔에 출력
# - insert, select, update 등 실제 실행되는 쿼리문 확인 가능
spring.jpa.show-sql=true

# MySQL8용 문법 규칙 설정
# - Hibernate(ORM 도구)가 DB에 맞는 SQL 문법을 사용하도록 함
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# 🚨 트랜잭션 및 JDBC 상세 로그 추가
logging.level.org.springframework.transaction=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# =====================================
# ✅ 서버 포트 설정
# =====================================
# 8080 포트 충돌을 피하기 위해 8081 포트를 사용
server.port=8081

# ==================================
# Naver Cloud Platform (Maps)
# ==================================
# 🚨 따옴표 없이 Client ID 원본 값만 입력합니다.
naver.map.client-id=tbn355x42m

# 🚨 [수정] SecurityConfig.java와 충돌하는 전역 CORS 설정을 제거합니다.
# spring.web.cors.allowed-origins=http://localhost:5180,http://127.0.0.1:5180
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allow-credentials=true